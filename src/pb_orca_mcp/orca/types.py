"""ctypes.Structure mirrors of ORCA C structs.

Definitions come from PBORCA.H in the PowerBuilder SDK
(<PB_install>/SDK/Include/PBORCA.H). Each struct is annotated with the
corresponding ORCA function(s) that produce or consume it.

Implemented in phases 2-5 incrementally.
"""
