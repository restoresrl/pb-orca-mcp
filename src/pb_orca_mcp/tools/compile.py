"""MCP tools for compilation and rebuild — the core agentic loop.

Tools: pb_compile_entry_import, pb_compile_entry_import_list,
pb_application_rebuild, pb_get_last_compile_errors.

Implemented in phase 5. This is the highest-value area of the project:
it closes the "edit → compile → read errors → fix" loop for PB.
"""
